Usar `scripts/v02_edge_cases.sh` para:

- projeto inativo
- telefone em `tn_optouts`
- `daily_limit = 0`
- `monthly_limit = 0`
- limite diário atingido
- limite mensal atingido

Usar `scripts/panel_smoke.sh` para:

- login/logout do painel
- bloqueio por CSRF em POST administrativo
